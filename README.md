# Rage

**RAGE AGAINST THE COMPILER**

## Getting Started

### Bootstrapping
```
cargo run --manifest-path ./bootstrap/Cargo.toml --release
```
Note: `--release` recommended for performance. Log level defaults to `INFO` for release mode and `DEBUG` otherwise.

