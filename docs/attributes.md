# Rage Attributes

## About
A better pre-processor hint.

## Standard Attributes

Attributes that come with default with the language.

- `#[inline]`, `#[inline(false)]`, or `#[inline(true)]` : Suggest, restrict, or force the compiler to inline the following code.
